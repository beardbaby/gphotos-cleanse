# Backend dependencies for the Duplicate Finder.
# Install with:  pip install -r requirements.txt
# (torch is large; if install fails on your platform, see https://pytorch.org for
#  the right install command, then re-run this file.)

fastapi>=0.136.3
uvicorn[standard]>=0.49.0
requests>=2.34.2
Pillow>=12.2.0
ImageHash>=4.3.2
pybktree>=1.1
numpy>=2.4.6
torch>=2.12.1
# torchvision is required by transformers' AutoImageProcessor (the DINOv2 stage).
# Left unpinned so pip resolves the build that matches the installed torch.
torchvision
transformers>=5.12.1
google-auth>=2.53.0
google-auth-oauthlib>=1.2.0
